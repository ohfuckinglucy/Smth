git init создать репозиторий
git config user.name/user.email "name" sds@gmail.com создать свой ник
cat .git/config отобразить конфиг

--system системная настройка
--global глобальная настройка
--local проектные настрйоки

git config --global core.editor 'путь' выбрать редактор
git config --global alios.c config теперь git config = git c

git add для добавления индекса
git status чтобы посмотреть что добавлено что нет
git commit для финиша

git config core.fileMode false на файлы файловой системы смотреть не надо
git update-index --chmod=+x index.html поставить доступ для юникса из под винды
так же этот флаг можно юзать для git add